################################################################################
# BLASTOISE DOCKER ENVIRONMENT CONFIGURATION
################################################################################
#
# ⚠️  SECURITY WARNING: This file contains DEMO credentials for local development.
#
# For LOCAL development:
#   1. Copy this file: cp .env.example .env
#   2. The default values are safe for local development
#   3. Start services: docker-compose up -d
#
# For PRODUCTION (Railway/Cloud):
#   ⚠️  YOU MUST GENERATE NEW SECRETS! Do NOT use these demo values.
#
#   Required changes:
#   1. Generate a new JWT_SECRET (32+ characters)
#   2. Change POSTGRES_PASSWORD to a strong password
#   3. Generate new ANON_KEY and SERVICE_ROLE_KEY
#      See: https://supabase.com/docs/guides/self-hosting#api-keys
#   4. Update kong.yml with your new keys
#
# See docker/README.md for detailed production setup instructions.
################################################################################

# Postgres Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres  # ⚠️  CHANGE THIS FOR PRODUCTION
POSTGRES_DB=postgres
POSTGRES_PORT=5432

# Redis Configuration
REDIS_PORT=6379

# JWT Configuration
# ⚠️  PRODUCTION: Generate a new random secret (32+ characters)
JWT_SECRET=super-secret-jwt-token-with-at-least-32-characters-long
JWT_EXPIRY=3600

# Supabase JWT Keys
# ⚠️  PRODUCTION: These are PUBLIC Supabase demo keys. Generate new ones!
# ⚠️  Use: jwt encode --secret="YOUR_JWT_SECRET" --exp="+10 years" '{"role":"anon","iss":"supabase"}'
# ⚠️  Also update the corresponding keys in kong.yml (lines 96 & 99)
ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU

# Kong Gateway Ports
KONG_HTTP_PORT=8000
KONG_HTTPS_PORT=8443

# Service Ports (usually don't need to expose these - Kong routes to them)
POSTGREST_PORT=3001
AUTH_PORT=9999
STORAGE_PORT=5000
REALTIME_PORT=4000

# URLs
API_EXTERNAL_URL=http://localhost:8000
SITE_URL=http://localhost:4200
ADDITIONAL_REDIRECT_URLS=

# Email Configuration (for auth)
ENABLE_EMAIL_SIGNUP=true
ENABLE_EMAIL_AUTOCONFIRM=true
DISABLE_SIGNUP=false
SMTP_ADMIN_EMAIL=admin@example.com
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SENDER_NAME=

# Mailer URL Paths
MAILER_URLPATHS_INVITE=/auth/v1/verify
MAILER_URLPATHS_CONFIRMATION=/auth/v1/verify
MAILER_URLPATHS_RECOVERY=/auth/v1/verify
MAILER_URLPATHS_EMAIL_CHANGE=/auth/v1/verify
