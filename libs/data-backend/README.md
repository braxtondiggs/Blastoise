# data-backend

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build data-backend` to build the library.

## Running unit tests

Run `nx test data-backend` to execute the unit tests via [Jest](https://jestjs.io).
